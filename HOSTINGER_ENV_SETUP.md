# Hostinger Environment Variables Setup

## 🎯 Цель

Настроить переменные окружения через панель Hostinger, чтобы **НЕ нужно было создавать `.env` файл вручную** после каждого redeploy.

---

## ✅ Преимущества использования Hostinger Environment Variables

1. **Не удаляются при redeploy** - переменные хранятся в настройках Node.js App, а не в файлах
2. **Безопаснее** - не нужно хранить `.env` на сервере
3. **Проще управлять** - изменения через веб-интерфейс без SSH
4. **Автоматически применяются** - при каждом запуске приложения

---

## 📝 Инструкция по настройке

### Шаг 1: Откройте настройки Node.js App

1. Зайдите в **Hostinger Panel** (hpanel.hostinger.com)
2. Выберите ваш хостинг
3. Перейдите в **Node.js** (боковое меню)
4. Выберите ваше приложение (api.parvaly.com)
5. Нажмите **Settings and redeploy**

### Шаг 2: Добавьте Environment Variables

Прокрутите вниз до секции **Environment variables**.

Нажмите **+ Add variable** и добавьте следующие переменные:

#### Обязательные переменные

```
NODE_ENV = production
PORT = 3000
```

#### Database Configuration

```
DB_HOST = 127.0.0.1
DB_PORT = 3306
DB_NAME = u707052196_parvaly_blog
DB_USER = u707052196_sergei
DB_PASSWORD = Kotelok00200!
```

#### JWT & Security

```
JWT_SECRET = rQOA9OnjHoPGsMs646/h5yEd39satlRjGLJ0rTNps2w=
JWT_EXPIRES_IN = 7d
SESSION_SECRET = B0CdMAqKM1Chk4PjC59jDnUZHvpl9MPjxYR9RUdiGAU=
```

#### Admin Credentials

```
ADMIN_USERNAME = admin
ADMIN_PASSWORD = parvaly2026
ADMIN_EMAIL = admin@parvaly.com
```

#### File Upload

```
MAX_FILE_SIZE = 5242880
ALLOWED_FILE_TYPES = image/jpeg,image/png,image/webp,image/svg+xml
```

#### Site Configuration

```
SITE_URL = https://parvaly.com
SITE_NAME = PARVALY
```

#### Security & Rate Limiting

```
RATE_LIMIT_WINDOW = 15
RATE_LIMIT_MAX_REQUESTS = 5
```

#### Backup Configuration (опционально)

```
BACKUP_PATH = ./backups
BACKUP_RETENTION_DAYS = 30
```

### Шаг 3: Сохраните и перезапустите

1. Нажмите **Save** внизу страницы
2. Нажмите **Update and redeploy** для применения изменений
3. Дождитесь завершения redeploy (~1-2 минуты)

### Шаг 4: Проверка

После redeploy проверьте логи приложения:

```
📝 Environment loaded from: system environment variables
🚀 API Server running on port 3000
✅ Ready to accept requests
```

Если видите `system environment variables` - всё работает! ✅

---

## 🔄 Как это работает

### 1. При старте приложения

```bash
npm start
  ↓
prestart: node api/scripts/ensure-env.js  ← Проверка конфигурации
  ↓
start: node api/server.js                  ← Запуск сервера
```

### 2. Скрипт `ensure-env.js` проверяет:

```
1. Есть ли .env файл?
   ├─ Да → ✅ Использует .env
   └─ Нет → Проверяет Hostinger env vars
              ├─ Да → ✅ Использует Hostinger env
              └─ Нет → ❌ Создает .env из .env.example
```

### 3. Приоритет загрузки

```
1️⃣ Hostinger Environment Variables (высший приоритет)
   ↓
2️⃣ .env файл (если существует)
   ↓
3️⃣ .env.example (fallback шаблон)
```

**Важно:** Если переменная установлена в Hostinger, она **НЕ будет перезаписана** значением из `.env` файла.

---

## 🛡️ Безопасность

### Что делать с `.env` файлом на сервере?

После настройки Hostinger Environment Variables:

**Вариант A (рекомендуется):** Удалить `.env` с сервера
```bash
# По SSH
rm /home/u707052196/domains/api.parvaly.com/public_html/.env
```

**Вариант B:** Оставить как резервную копию
- Приложение будет игнорировать `.env`, если Hostinger env vars установлены
- `.env` защищен `.gitignore` от коммитов

### Защита паролей

✅ **Правильно:**
- Использовать Hostinger Environment Variables
- Переменные хранятся зашифрованными в панели
- Не видны через File Manager

❌ **Неправильно:**
- Хранить `.env` в публичной директории
- Коммитить `.env` в git
- Передавать пароли в plaintext

---

## 🔧 Troubleshooting

### Проблема: "Missing required environment variables"

**Причина:** Переменные не установлены ни в Hostinger, ни в `.env`

**Решение:**
1. Проверьте настройки Node.js App → Environment variables
2. Убедитесь, что все обязательные переменные добавлены
3. Нажмите "Update and redeploy"

### Проблема: "Cannot connect to database"

**Причина:** Неправильные DB credentials

**Решение:**
1. Проверьте `DB_HOST` (должен быть `127.0.0.1` для Hostinger)
2. Проверьте `DB_NAME`, `DB_USER`, `DB_PASSWORD`
3. В Hostinger Panel → Databases → MySQL проверьте правильные имена

### Проблема: Приложение использует старые значения

**Причина:** Изменения в Environment Variables не применились

**Решение:**
1. После изменения переменных ОБЯЗАТЕЛЬНО нажмите **Update and redeploy**
2. Или перезапустите приложение вручную в панели
3. Проверьте логи: должны обновиться значения

### Проблема: "Environment loaded from: .env file" вместо "system environment variables"

**Причина:** `.env` файл существует на сервере и имеет приоритет

**Решение (если хотите использовать Hostinger env vars):**
1. Удалите `.env` с сервера
2. Убедитесь, что все переменные установлены в Hostinger
3. Перезапустите приложение

---

## 📚 Дополнительные команды

### Проверить конфигурацию вручную

```bash
# По SSH на сервере
cd /home/u707052196/domains/api.parvaly.com/public_html
npm run check-env
```

Вывод:
```
🔍 Checking environment configuration...
✅ Using environment variables from Hostinger Node.js App settings
💡 Tip: .env file is optional when using Hostinger Environment Variables
```

### Создать .env из шаблона (если нужно)

```bash
cp .env.example .env
nano .env  # Отредактировать значения
chmod 600 .env  # Установить безопасные права
```

---

## 🎓 Best Practices

### ✅ Рекомендуется

1. **Использовать Hostinger Environment Variables** для production
2. **Использовать `.env` файл** только для local development
3. **Хранить `.env.example`** в git как шаблон (без секретов)
4. **Никогда не коммитить** реальный `.env` в git

### ❌ Избегать

1. Хардкодить secrets в коде
2. Хранить пароли в plaintext файлах
3. Использовать одинаковые секреты для dev и prod
4. Шарить `.env` файлы через Slack/Email

---

## 📖 Связанные документы

- [ANTI_REDEPLOY_SOLUTION.md](./ANTI_REDEPLOY_SOLUTION.md) - Как избежать затирания файлов при redeploy
- [DEPLOYMENT.md](./DEPLOYMENT.md) - Общее руководство по деплою
- [PRODUCTION_GUIDE.md](./PRODUCTION_GUIDE.md) - Production best practices

---

## ✅ Чеклист настройки

После выполнения всех шагов, проверьте:

- [ ] Все переменные добавлены в Hostinger Environment Variables
- [ ] Нажата кнопка "Save" и "Update and redeploy"
- [ ] В логах видно: "Environment loaded from: system environment variables"
- [ ] API отвечает на `https://api.parvaly.com/api/health`
- [ ] Админка работает на `https://parvaly.com/admin/login.html`
- [ ] Логин в админку проходит успешно
- [ ] Можно создавать/редактировать статьи

---

**Готово!** Теперь ваше приложение будет работать с переменными окружения из Hostinger, и вам НЕ нужно будет создавать `.env` файл после каждого redeploy. 🎉
